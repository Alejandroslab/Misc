first run

docker ps -a

to check all your docker processes

you will see that they are 'exited' some time ago




docker start ContainerName

or just run:

docker start ID


where 'ID' and 'ContainerName' are the alphanumeric long ID (impossibile to rememeber) and the name that you set up when you run it for the first time (you can retrieve the ID and the name also by the command 'docker ps -a' under the 'ID' and 'NAME' columns )

when you start a container you may want to wait some minutes before accessing it. as it may occur some time 